{
  "name": "@johndavis.dev/95horatio",
  "version": "0.1.0",
  "description": "Monorepo package for the Prometheus ML engine.",
  "main": "index.js",
  "author": "John Davis",
  "license": "MIT",
  "scripts": {
    "bootstrap": "lerna bootstrap --no-ci --ignore-scripts --concurrency 1",
    "build": "lerna run build",
    "clean": "lerna run clean --parallel",
    "clean-reset": "lerna run reset --parallel",
    "test": "lerna run test",
    "release": "npm run bootstrap && npm run build && npm run test"
  },
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
